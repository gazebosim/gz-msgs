load(
    "//ign_bazel:cmake_configure_file.bzl",
    "cmake_configure_file",
)

BINARY_DIR = "/tmp"
SOURCE_DIR = "/tmp"

# Generates config.hh based on the version numbers in CMake code.
cmake_configure_file(
    name = "test_config",
    src = "test_config.h.in",
    out = "include/ignition/msgs/test_config.h",
    cmakelists = ["CMakeLists.txt"],
    defines = [
        "CMAKE_BINARY_DIR=%s" % (BINARY_DIR),
        "PROJECT_BINARY_DIR=%s" % (BINARY_DIR),
        "PROJECT_SOURCE_DIR=%s" % (SOURCE_DIR),
    ],
    visibility = ["//visibility:private"],
)

cc_library(
    name = "test_utils",
    srcs = ["include/ignition/msgs/test_config.h"],
    includes = ["include"],
    visibility = ["//visibility:public"],
)

